# Exports frame with given index eg. 0 is the first frame and -1 is the last frame
# Each frame should contain data from all the sensors.
# If Ouster starts later than Baslers frame = 0 does not m

main(rec_path, frame)